dropout = 50

# total number of students enrolled
enrolled = 10

# calculate percentage dropout
percent_dropout = (dropout/enrolled) * 100

# print percentage dropout formatted as a string with 2 decimal places
print(f"Percentage dropout: {percent_dropout:.2f}%")